import styled from 'styled-components';

const Link = styled('a')`
  color: #fff;
  display: block;
  font-weight: normal;
  padding: 6px 6px;
  position: relative;

  &:hover {
    text-decoration: underline;
  }
`;

export const Styles = { Link };
